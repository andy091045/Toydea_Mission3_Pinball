using System.Collections;
using System.Collections.Generic;
using NUnit.Framework;
using UnityEngine;
using UnityEngine.TestTools;
using GameManagerNamespace;
using MissionNamespace;
using ScoreManagerNamespace;
using System;

public class GameManagerTest
{
    
    [Test]
    [TestCase()]
    public void GameManager_Function_ReStart_Test()
    {
        /*
        var gameObject = new GameObject();
        var gameInput = gameObject.AddComponent<GameInput>();
        var gameManager = gameObject.AddComponent<GameManager>();

        gameInput.TotalScore = 10;
        gameInput.Lifetimes = 10;
        gameManager.ReStart();

        Assert.That(gameInput.TotalScore, Is.EqualTo(0));
        Assert.That(gameInput.Lifetimes, Is.EqualTo(5));
        */
    }
    
}





